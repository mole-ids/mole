interface:
    iface: ens33
    pf_ring: true
    bpf:

engine:

rules:
    rules_dir: ./rules
    rules_index:

logger:
    log_to: /dev/stdout
    log_level: "info"

debug:
  dot:
    output: stdout
    rules_dir: ./rules
    rules_index:
